

You're now finishing the rebranding and polish of the **Ergovia-AI** platform. Please perform the following tasks across all pages and components:

---

### 🪪 1. **Rename All Instances of “Prismity” to “Ergovia”**

* Search and replace all:

  * `Prismity`, `Prismity.AI`, `PRISMiTY.AI`, etc.
  * Update to: `Ergovia` or `Ergovia-AI` (based on usage/context)
* This includes headings, tooltips, alt text, comments, and embedded copy

---

### 🎨 2. **Fix `.auth-logo-text` Visibility Issue**

* Current:

  ```html
  <span class="auth-logo-text">PRISMiTY.AI</span>
  ```

  is invisible due to outdated color or gradient

* ✅ Fix:

  * Update the span to show “**Ergovia-AI**”
  * Apply a **visible gradient** using current brand colors:

    * `#F9F2E8` (cream)
    * `#9E691B` (deep gold)
    * `#CAD3DE` (muted light gray-blue)
  * Add proper fallback font color and sizing
  * Make sure it's readable in both light and dark modes

---

### 🌀 3. **Fix “Snapping” / Visual Flash on Load**

* Some pages still show a brief flash of old (blue/purple) styling before the Ergovia theme applies.
* ✅ What to do:

  * Apply the theme class or `data-theme="dark"` immediately in the `<html>` tag
  * Inline critical CSS for theme variables in the `<head>`
  * Remove any leftover fallback styles or animations that use violet

---

### 🔗 4. **Fix URL Paths Ending with `.html`**

* Many internal links still display `.html` in the browser URL (e.g. `/dashboard.html`, `/templates.html`)
* ✅ Goal:

  * Make URLs clean like `/dashboard`, `/templates`, etc.
* ✅ Fix:

  * Configure routing or .htaccess (if applicable) to rewrite URLs
  * Use `history.pushState()` in JavaScript (if SPA-style app)
  * Avoid breaking browser reloads or deep linking

---

### 🧭 5. **Set Browser Tab Icon to Ergovia Logo**

* ✅ Goal:

  * Display `Logo.PNG` in the browser tab as the favicon
* ✅ How:

  * Convert `Logo.PNG` to `favicon.ico` (or use it directly as PNG)
  * In your `<head>`, add:

    ```html
    <link rel="icon" href="/assets/Logo.PNG" type="image/png">
    ```
  * Ensure the path is correct and the file is available on all pages

---

### 📌 Final Notes:

* This is a **finishing pass** — focus only on brand rename, visual polish, and link structure
* Do **not change core logic, GPT flows, or Stripe/payment behavior**
* Please return:

  * Updated HTML/CSS
  * Any JS routing changes
  * A brief summary of what was fixed and confirmed working

